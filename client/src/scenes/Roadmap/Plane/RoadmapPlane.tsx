import React, { FC } from 'react';
import { Quarter } from '@interfaces';

interface Props {
  quarters: Quarter;
}

const RoadmapPlane: FC<Props> = (props) => (
  <div>Quarters</div>
);

export default RoadmapPlane;
